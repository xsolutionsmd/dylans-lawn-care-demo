# Design workflow setup validation

September 18, 2026; tooling/inspo-dev from dev 4de99f0. Added portable repo skill,
project-specific context, connector helper and README/agent routes. No dist,
booking code, image inputs or deployment workflow changed. Root static-image
context remains deny-by-default; the booking build uses its separate booking context.

Observed: public MCP initialization and all 15 tools discovered; a synthetic
landscape-services brief returned recommendation text and three image blocks.
An actual image was opened (a landscape-led architecture reference). Results also
included unrelated examples, so relevance checks and a bounded fallback are part
of the skill. No private client data was sent. Python helper syntax, Markdown
routes and skill validation passed. Actual Codex skills/list found the enabled
repo skill; hosted connector setup is idempotent. Evidence-directory collision
refusal preserved prior results. Desktop hot reload is not claimed; the direct
helper works immediately and native MCP tools may require a new session.

This verifies tooling and discovery, not a new site design, booking behavior,
visual-quality benchmark or five-minute turnaround. Existing release checks still
apply to future application changes. Dev pushes do not deploy automatically.
